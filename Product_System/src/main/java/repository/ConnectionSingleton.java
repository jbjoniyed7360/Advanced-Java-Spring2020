package repository;import java.sql.Connection;import java.sql.DriverManager;import java.sql.SQLException;public class ConnectionSingleton {    private static final String HOSTNAME = "123.200.8.61";    private static final String DBNAME = "productdb";    private static final String DBURL = "jdbc:mysql://" + HOSTNAME + "/" + DBNAME;    private static final String USERNAME = "cse4047";    private static final String PASS = "Lazy_Day_33";    private  static Connection connection = null;    private static final ConnectionSingleton connectionSingleton = new ConnectionSingleton();    private ConnectionSingleton(){        try {            connection = DriverManager.getConnection(DBURL,USERNAME,PASS);            System.out.println("Successfully Connected...");        } catch (SQLException e) {            e.printStackTrace();        }    }    public static Connection getConnection(){        return connection;    }}