package bd.edu.seu.vaadingdemo.ui;import bd.edu.seu.vaadingdemo.model.Member;import bd.edu.seu.vaadingdemo.model.Student;import bd.edu.seu.vaadingdemo.repository.MemberRepository;import com.vaadin.flow.component.button.Button;import com.vaadin.flow.component.formlayout.FormLayout;import com.vaadin.flow.component.grid.Grid;import com.vaadin.flow.component.html.Label;import com.vaadin.flow.component.orderedlayout.VerticalLayout;import com.vaadin.flow.component.textfield.TextField;import com.vaadin.flow.data.binder.Binder;import com.vaadin.flow.router.Route;import java.awt.*;@Route("member")public class MemberView extends VerticalLayout {    private MemberRepository memberRepository;//    private Binder<Member> memberBinder;    public MemberView(MemberRepository memberRepository) {        this.memberRepository = memberRepository;//        memberBinder = new Binder<>();        TextField idField = new TextField("Id:","id");        TextField nameField = new TextField("Name:","name");        TextField phoneField = new TextField("Phone:","phone");        TextField addressField = new TextField("Address:","address");        TextField bloodGroupField = new TextField("Blood Group:","blood");        TextField ageField = new TextField("Age:","age");        TextField sexField = new TextField("Sex:","sex");        TextField occupationField = new TextField("Occupation","occupation");        TextField gradeField = new TextField("grade","grade");        Button save = new Button("Save");        FormLayout formLayout = new FormLayout();        formLayout.add(idField,nameField,phoneField,addressField,bloodGroupField,ageField,sexField,occupationField,gradeField,save);//        memberBinder.bind(nameField,Member::getName,Member::setName);//        memberBinder.bind(addressField,Member::getAddress,Member::setAddress);//        memberBinder.bind(sexField,Member::getSex,Member::setSex);        Label label = new Label("আলোর সন্ধানে ধরখার");        Grid<Member> memberGrid = new Grid<>(Member.class);        memberGrid.setItems(memberRepository.findAll());        save.addClickListener(event -> {            long id = Long.parseLong(idField.getValue());            String name = nameField.getValue();            String phone = phoneField.getValue();            String address = addressField.getValue();            String bloodgroup = bloodGroupField.getValue();            double age = Double.parseDouble(ageField.getValue());            String sex = sexField.getValue();            String occupation = occupationField.getValue();            String grade = gradeField.getValue();            Member member = new Member(id,name,phone,address,bloodgroup,age,sex,occupation,grade);            memberRepository.save(member);            memberGrid.setItems(memberRepository.findAll());        });        memberGrid.addItemClickListener(event -> {            Member member = event.getItem();//            memberBinder.readBean(member);            idField.setValue(String.valueOf(member.getA_id()));            nameField.setValue(member.getName());            phoneField.setValue(member.getPhone());            addressField.setValue(member.getAddress());            bloodGroupField.setValue(member.getBloodGroup());            ageField.setValue(String.valueOf(member.getAge()));            sexField.setValue(member.getSex());            occupationField.setValue(member.getOccupation());            gradeField.setValue(member.getGrade());        });        add(label,formLayout,memberGrid);    }}